/*
  Sensitive data is overridden when deploying on vps
  Values provided may or may not work on the host
  For all sensitive data (i.e. db passwords), gh secrets
  are used to feed data into the vm when publishing
*/
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ConnectionStrings": {
    "MySql": "server=localhost;uid=root;database=personal_website_testing",
    "Redis": "localhost:6002"
  }
}
